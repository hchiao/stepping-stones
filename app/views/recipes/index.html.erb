<h1>Listing recipes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
        <td><%= recipe.name %></td>
        <!--
        <td><%#= link_to 'Show', recipe %></td>
        <td><%#= link_to 'Edit', edit_recipe_path(recipe) %></td>
        <td><%#= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        -->
        <td><%= link_to 'Follow recipe', path_client_form_path(:view_recipe => recipe), class: "ui-button" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recipe', path_customize_path_path  %>
